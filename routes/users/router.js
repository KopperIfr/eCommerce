/**
 * User Router
 */

const express = require('express');
const { Router } = express;

const router = Router();

router.get('/sign-out', );
router.post('/sign-up',);
router.post('/sign-in', );

module.exports = router;